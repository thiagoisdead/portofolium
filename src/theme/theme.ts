// src/theme.ts
import { createTheme } from '@mui/material/styles';

// Crie um tema personalizado com sua fonte
const theme = createTheme({
    typography: {
        fontFamily: 'Pragmatica-Extended-Bold, Arial, sans-serif', // Defina sua fonte personalizada aqui
    },
});

export default theme;
